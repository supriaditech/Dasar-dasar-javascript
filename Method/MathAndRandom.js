console.log(Math.PI); // Menampilkan nilai PI (~3.14)
console.log(Math.round(1.6)); // Membulatkan angka ke nilai terdekat, dalam hal ini menjadi 2
console.log(Math.round(1.4)); // Membulatkan angka ke nilai terdekat, dalam hal ini menjadi 1
console.log(Math.ceil(1.2)); // Membulatkan angka ke atas, menjadi 2
console.log(Math.floor(1.8)); // Membulatkan angka ke bawah, menjadi 1
console.log(Math.min(1, 2, 3, 4, 5)); // Menemukan nilai terkecil dari daftar angka, yaitu 1
console.log(Math.max(1, 2, 3, 4, 5)); // Menemukan nilai terbesar dari daftar angka, yaitu 5
console.log(Math.random()); // Menghasilkan angka acak antara 0 (inklusif) dan 1 (eksklusif)
